# macOS
.DS_Store

# Plaintext env files should never be committed
.env
.env.missing
.env.local
.env.*.local

# age private key should never be committed
age-key.txt

# Encrypted env files are safe to commit
!.env.age
!.env.*.age

# Node
web/node_modules/
web/dist/

# Superpowers brainstorming
.superpowers/

# Slidev build output (generated, not tracked)
web/public/slides/

# Video assets (hosted via NAS bind-mount, rsynced separately)
web/public/videos/

# TypeScript build info
web/tsconfig.tsbuildinfo
server/node_modules/

# 北市 EMTP CIT 教材庫 — 31GB 參考資料庫，本地保留但不 commit
北市EMTP_CIT相關文件/
